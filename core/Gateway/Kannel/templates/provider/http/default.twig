
group=smsc
smsc=http
smsc-id=MT2MO
max-pending-submits=10
system-type=kannel
smsc-username={{ provider.username }}
smsc-password={{ provider.password }}
port={{ provider.port }}
send-url="{{ provider.url }}"
{% if provider.settings is iterable %}
  {% for key, value in provider.settings %}
    {% if key != 'name' %}
{{ key }}={{ value }}
    {% endif %}
  {% endfor %}
{% endif %}
